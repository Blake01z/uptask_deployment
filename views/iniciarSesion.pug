doctype html
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario    
            h1 Iniciar Sesion en Uptask

            if locals.mensajes
                .alerta
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                            p=error

            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo 
                    label(for="email") Email: 
                    input(type="email" name="email" id="email" placeholder="Tu email" required value=email)
                .campo 
                    label(for="password") Password: 
                    input(type="password" name="password" id="password" placeholder="Tu password" required value=password)
                .campo.enviar 
                    input.boton(type="submit" value="Iniciar Sesion")
                .campo.acciones 
                    a(href="/crear-cuenta") ¿Aun no tienes una cuenta? Crea una gratis

                    a(href="/reestablecer") ¿Olvidaste tu contraseña? 

    script(src="/dist/bundle.js")