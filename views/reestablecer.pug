doctype html
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario 
            h1= nombrePagina 
            p Agrega tu e-mail para reestablecer tu contraseña

            if locals.mensajes
                .alerta
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p=error

            form.caja-login(action="/reestablecer" method="POST") 
                .campo 
                    label(for="email") Email: 
                    input(type="email" name="email" id="email" placeholder="Tu email" required value=email)
                .campo.enviar 
                    input.boton(type="submit" value="Restablecer Password")
                .campo.acciones
                    a(href="/iniciar-sesion") He recordado el password 